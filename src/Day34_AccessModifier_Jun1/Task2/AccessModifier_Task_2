create class Student
with private 1 fields : name, age

also, create private "default constructor"
and public constructor that initialize all attributes

create objects of Students class in Task2.java